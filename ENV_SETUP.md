# Настройка переменных окружения (.env)

## 🔐 Важно!

Файл `.env` содержит конфиденциальную информацию и **НЕ должен** попадать в Git.

## 📝 Создание .env файла

### Способ 1: Ручное создание

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# База данных
DATABASE_URL=sqlite:///./app.db

# JWT секретный ключ (ОБЯЗАТЕЛЬНО измените!)
JWT_SECRET=your-secret-key-change-in-production-min-32-chars

# Время жизни JWT токена в минутах (1440 = 24 часа)
JWT_EXPIRE_MINUTES=1440

# Опционально: данные первого администратора
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
```

### Способ 2: Через командную строку

**macOS/Linux:**
```bash
cat > .env << EOF
DATABASE_URL=sqlite:///./app.db
JWT_SECRET=your-secret-key-change-in-production-min-32-chars
JWT_EXPIRE_MINUTES=1440
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
EOF
```

**Windows PowerShell:**
```powershell
@"
DATABASE_URL=sqlite:///./app.db
JWT_SECRET=your-secret-key-change-in-production-min-32-chars
JWT_EXPIRE_MINUTES=1440
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
"@ | Out-File -FilePath .env -Encoding UTF8
```

### Способ 3: Генерация безопасного JWT_SECRET

**Python:**
```python
import secrets
print(secrets.token_urlsafe(32))
```

**macOS/Linux:**
```bash
openssl rand -base64 32
```

**PowerShell:**
```powershell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
```

## 📋 Описание переменных

### DATABASE_URL
**Описание**: Путь к базе данных SQLite.

**Значение по умолчанию**: `sqlite:///./app.db`

**Варианты**:
- `sqlite:///./app.db` - файл в корне проекта
- `sqlite:////tmp/app.db` - файл в /tmp
- `sqlite:///path/to/app.db` - абсолютный путь

**Важно**: Для SQLite используется 3 слеша `///` для относительного пути и 4 слеша `////` для абсолютного.

---

### JWT_SECRET
**Описание**: Секретный ключ для подписи JWT токенов.

**Требования**:
- Минимум 32 символа
- Уникальный для каждого проекта
- Не должен быть предсказуемым

**⚠️ КРИТИЧЕСКИ ВАЖНО**:
- **НЕ используйте** пример из документации в продакшене!
- **ВСЕГДА генерируйте** уникальный ключ!
- **НИКОГДА** не коммитьте в Git!

**Примеры генерации**:
```python
# Python
import secrets
secrets.token_urlsafe(32)
# Результат: 'dQw4w9WgXcQ_r7LpqK9w8YhJ5rVfNmPqA1bC2dE3fG'
```

```bash
# Linux/macOS
openssl rand -hex 32
# Результат: '4f8a5b2c9d1e3f7a6b8c0d2e4f6a8b0c2d4e6f8a0b2c4d6e8f0a2b4c6d8e0f2a'
```

---

### JWT_EXPIRE_MINUTES
**Описание**: Время жизни JWT токена в минутах.

**Значение по умолчанию**: `1440` (24 часа)

**Рекомендации**:
- Разработка: 1440 минут (24 часа) - удобно для тестирования
- Продакшен: 60-180 минут (1-3 часа) - безопаснее
- Короткие сессии: 15-30 минут - максимальная безопасность

**Примеры**:
```env
JWT_EXPIRE_MINUTES=15      # 15 минут
JWT_EXPIRE_MINUTES=60      # 1 час
JWT_EXPIRE_MINUTES=180     # 3 часа
JWT_EXPIRE_MINUTES=1440    # 24 часа
JWT_EXPIRE_MINUTES=10080   # 7 дней
```

---

### ADMIN_EMAIL (опционально)
**Описание**: Email первого администратора.

**Значение по умолчанию**: `admin@example.com`

**Использование**: Переменная пока не используется в коде, но доступна для будущих расширений.

**Примеры**:
```env
ADMIN_EMAIL=admin@mycompany.com
ADMIN_EMAIL=superuser@example.org
```

---

### ADMIN_PASSWORD (опционально)
**Описание**: Пароль первого администратора.

**Значение по умолчанию**: `admin123`

**Использование**: Переменная пока не используется в коде, но доступна для будущих расширений.

**⚠️ Безопасность**:
- В продакшене используйте сложный пароль
- Минимум 12 символов
- Комбинация букв, цифр, символов

**Примеры**:
```env
ADMIN_PASSWORD=MySecureP@ssw0rd2024!
ADMIN_PASSWORD=Tr0ng_P@ssw0rd_H3re!
```

## 📁 Пример полного .env файла

### Для разработки (Development)

```env
# === Конфигурация для разработки ===

# База данных (локальный файл)
DATABASE_URL=sqlite:///./app.db

# JWT ключ (генерируйте свой!)
JWT_SECRET=dev-secret-key-CHANGE-THIS-32-characters-minimum

# Долгое время жизни токена для удобства разработки
JWT_EXPIRE_MINUTES=1440

# Тестовые данные администратора
ADMIN_EMAIL=admin@localhost.dev
ADMIN_PASSWORD=admin123
```

### Для продакшена (Production)

```env
# === Конфигурация для продакшена ===

# База данных (можно использовать полный путь)
DATABASE_URL=sqlite:////var/lib/taskmanager/app.db

# JWT ключ (ОБЯЗАТЕЛЬНО уникальный и сложный!)
JWT_SECRET=Xy9ZpQ2rT5wU8xA1bC4dE7fG0hJ3kL6mN9oP2qR5sT8uV1wX4yZ7aB0cD3eF6gH9

# Короткое время жизни токена для безопасности
JWT_EXPIRE_MINUTES=180

# Данные реального администратора
ADMIN_EMAIL=admin@company.com
ADMIN_PASSWORD=SuperSecureP@ssw0rd2024!
```

## 🔍 Проверка .env файла

После создания файла проверьте его:

```bash
# Проверить существование файла
ls -la .env

# Посмотреть содержимое (осторожно, секретные данные!)
cat .env

# Проверить, что файл не в Git
git status
# Должно быть: .env в списке untracked files или не отображаться (если в .gitignore)
```

## ✅ Проверка загрузки переменных

Создайте файл `test_env.py`:

```python
from app.core.config import settings

print(f"Database URL: {settings.DATABASE_URL}")
print(f"JWT Secret: {settings.JWT_SECRET[:10]}...") # Показываем только начало
print(f"JWT Expire: {settings.JWT_EXPIRE_MINUTES} minutes")
print(f"Admin Email: {settings.ADMIN_EMAIL}")
```

Запустите:
```bash
python test_env.py
```

Если всё работает, увидите значения из .env файла.

## ⚠️ Безопасность

### ✅ Правильно

- ✅ Создать `.env` локально
- ✅ Добавить `.env` в `.gitignore`
- ✅ Использовать уникальный `JWT_SECRET`
- ✅ Генерировать ключи криптографически безопасно
- ✅ Использовать разные ключи для dev/prod
- ✅ Хранить `.env` в безопасном месте
- ✅ Не отправлять `.env` в мессенджерах

### ❌ Неправильно

- ❌ Коммитить `.env` в Git
- ❌ Использовать слабый `JWT_SECRET`
- ❌ Использовать одинаковый ключ везде
- ❌ Публиковать `.env` в интернете
- ❌ Отправлять `.env` по email
- ❌ Хранить в публичных облаках

## 🆘 Troubleshooting

### Ошибка: "JWT_SECRET is required"

**Причина**: Файл `.env` не найден или переменная не указана.

**Решение**:
1. Убедитесь, что файл `.env` существует в корне проекта
2. Проверьте, что переменная `JWT_SECRET` указана
3. Перезапустите приложение

### Ошибка: "No such file or directory: '.env'"

**Причина**: Файл `.env` не создан.

**Решение**: Создайте файл по инструкциям выше.

### Переменные не загружаются

**Причина**: Файл `.env` в неправильном месте.

**Решение**: Файл должен быть в корне проекта, рядом с `app/`, `tests/`, `requirements.txt`.

```
task-managment-system-api/
├── .env          ← ЗДЕСЬ
├── app/
├── tests/
└── requirements.txt
```

### Токены быстро истекают

**Причина**: Слишком маленькое значение `JWT_EXPIRE_MINUTES`.

**Решение**: Увеличьте значение в `.env` файле.

## 📚 Дополнительные ресурсы

- [python-dotenv документация](https://github.com/theskumar/python-dotenv)
- [Pydantic Settings документация](https://docs.pydantic.dev/latest/usage/pydantic_settings/)
- [Генераторы паролей](https://www.random.org/passwords/)
- [Best practices для .env](https://12factor.net/config)

## 🎯 Checklist перед запуском

- [ ] Файл `.env` создан в корне проекта
- [ ] `DATABASE_URL` указан корректно
- [ ] `JWT_SECRET` сгенерирован уникальный (минимум 32 символа)
- [ ] `JWT_EXPIRE_MINUTES` установлен подходящий
- [ ] Файл `.env` добавлен в `.gitignore`
- [ ] Файл `.env` НЕ закоммичен в Git
- [ ] Переменные загружаются корректно (проверено)

---

**После настройки .env файла можно запускать приложение!**

```bash
python run.py
```

или

```bash
uvicorn app.main:app --reload
```

**Документация**: http://localhost:8000/docs

