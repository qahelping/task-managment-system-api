# 📚 Инструкция по развертыванию проекта локально

## Для студентов

Это пошаговое руководство поможет вам запустить Task Management System на своем компьютере.

---

## 📋 Содержание

1. [Работа с Docker](#работа-с-docker)
2. [Требования](#требования)
3. [Установка Backend (Python/FastAPI)](#установка-backend-pythonfastapi)
4. [Установка Frontend (React/Vite)](#установка-frontend-reactvite)
5. [Запуск проекта](#запуск-проекта)
6. [Проверка работы](#проверка-работы)
7. [Первые шаги](#первые-шаги)
8. [Решение проблем](#решение-проблем)

---

## 🐳 Работа с Docker

Если у вас установлен Docker и Docker Compose, вы можете запустить проект в контейнерах. Это самый простой способ развертывания.

### Требования для Docker

- **Docker Desktop** (Windows/Mac) или **Docker Engine** (Linux)
- **Docker Compose** (обычно входит в Docker Desktop)

Проверьте установку:
```bash
docker --version
docker-compose --version
```

### Запуск приложения через Docker

1. **Перейдите в корень проекта:**
   ```bash
   cd task-managment-system-api
   ```

2. **Запустите контейнеры:**
   ```bash
   docker-compose up --build
   ```

   Флаг `--build` пересоберет образы при первом запуске или после изменений.

3. **Для запуска в фоновом режиме (detached mode):**
   ```bash
   docker-compose up -d --build
   ```

4. **Проверьте статус контейнеров:**
   ```bash
   docker-compose ps
   ```

✅ После запуска приложение будет доступно:
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8000
- **API Документация:** http://localhost:8000/docs

### Остановка приложения

**Остановка с сохранением данных:**
```bash
docker-compose stop
```

**Остановка и удаление контейнеров (данные сохраняются в volumes):**
```bash
docker-compose down
```

**Остановка с удалением контейнеров и volumes (⚠️ удалит все данные БД!):**
```bash
docker-compose down -v
```

### Перезапуск приложения

**Перезапуск контейнеров:**
```bash
docker-compose restart
```

**Полный перезапуск (остановка + запуск):**
```bash
docker-compose down
docker-compose up -d
```

**Перезапуск с пересборкой образов:**
```bash
docker-compose up -d --build
```

### Очистка кэша Docker

**Очистка build кэша:**
```bash
docker builder prune
```

**Очистка всех неиспользуемых данных (образы, контейнеры, volumes, сети):**
```bash
docker system prune -a --volumes
```

⚠️ **Внимание:** Последняя команда удалит все неиспользуемые Docker ресурсы, включая volumes с данными БД!

**Очистка только build кэша для проекта:**
```bash
docker-compose build --no-cache
```

### Удаление контейнеров и образов

**Удаление контейнеров проекта:**
```bash
docker-compose down
```

**Удаление контейнеров и образов проекта:**
```bash
docker-compose down --rmi all
```

**Удаление конкретного контейнера:**
```bash
docker rm -f task-management-backend
docker rm -f task-management-frontend
```

**Удаление образов проекта:**
```bash
docker rmi task-managment-system-api-backend
docker rmi task-managment-system-api-frontend
```

### Просмотр логов

**Логи всех сервисов:**
```bash
docker-compose logs
```

**Логи конкретного сервиса:**
```bash
docker-compose logs backend
docker-compose logs frontend
```

**Логи в реальном времени (follow):**
```bash
docker-compose logs -f
```

**Последние 100 строк логов:**
```bash
docker-compose logs --tail=100
```

### Работа с базой данных в Docker

База данных SQLite хранится в Docker volume `db_data`. Данные сохраняются между перезапусками контейнеров.

**Просмотр volumes:**
```bash
docker volume ls
```

**Просмотр информации о volume:**
```bash
docker volume inspect task-managment-system-api_db_data
```

**Подключение к контейнеру для работы с БД:**
```bash
docker exec -it task-management-backend sh
```

Внутри контейнера база данных находится по пути: `/app/data/app.db`

### Накатывание дампа базы данных

#### Вариант 1: Через копирование файла БД в контейнер

1. **Остановите контейнеры:**
   ```bash
   docker-compose down
   ```

2. **Скопируйте файл дампа в volume:**
   ```bash
   # Создайте временный контейнер для доступа к volume
   docker run --rm -v task-managment-system-api_db_data:/data -v $(pwd):/backup alpine sh -c "cp /backup/app.db /data/app.db"
   ```

   **На Windows (PowerShell):**
   ```powershell
   docker run --rm -v task-managment-system-api_db_data:/data -v ${PWD}:/backup alpine sh -c "cp /backup/app.db /data/app.db"
   ```

3. **Запустите контейнеры:**
   ```bash
   docker-compose up -d
   ```

### Создание дампа базы данных

**Создание SQL дампа:**
```bash
docker exec task-management-backend sqlite3 /app/data/app.db .dump > backup.sql
```

**Копирование файла БД на хост:**
```bash
docker cp task-management-backend:/app/data/app.db ./backup_app.db
```

**Создание дампа через volume:**
```bash
docker run --rm -v task-managment-system-api_db_data:/data -v $(pwd):/backup alpine sh -c "cp /data/app.db /backup/app.db"
```

### Полезные команды Docker Compose

```bash
# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Выполнение команды в контейнере
docker-compose exec backend python fill_database.py

# Пересборка конкретного сервиса
docker-compose build backend

# Масштабирование (если нужно несколько экземпляров)
docker-compose up -d --scale backend=2

# Просмотр использования ресурсов
docker stats
```


## 🔧 Требования

Перед началом убедитесь, что у вас установлено:

- **Python 3.10 или выше** (проверьте: `python --version`)
- **Node.js 18 или выше** (проверьте: `node --version`)
- **npm** (обычно устанавливается вместе с Node.js)
- **Git** (для клонирования репозитория)

---

## 🐍 Установка Backend (Python/FastAPI)

### Шаг 1: Клонирование проекта

Откройте терминал и выполните:

```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd task-managment-system-api
```

### Шаг 2: Создание виртуального окружения

**На Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**На macOS/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

После активации в начале строки терминала появится `(venv)`.

### Шаг 3: Установка зависимостей Python

```bash
pip install -r requirements.txt
```

Это может занять несколько минут. Дождитесь завершения установки.

### Шаг 4: Создание файла .env

Создайте файл `.env` в корне проекта (там же, где находится `requirements.txt`).

**Содержимое файла .env:**

```env
DATABASE_URL=sqlite:///./app.db
JWT_SECRET=my-super-secret-key-change-this-in-production-min-32-chars-long
JWT_EXPIRE_MINUTES=1440
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
```

⚠️ **Важно:** Не удаляйте и не изменяйте этот файл после создания!

### Шаг 5: Проверка установки Backend

Выполните команду:

```bash
python -c "import fastapi; print('FastAPI установлен успешно!')"
```

Если видите сообщение об успехе, значит всё установлено правильно.

---

## ⚛️ Установка Frontend (React/Vite)

### Шаг 1: Переход в папку frontend

```bash
cd frontend
```

### Шаг 2: Установка зависимостей Node.js

```bash
npm install
```

Это может занять 2-5 минут. Дождитесь завершения установки.

### Шаг 3: Проверка установки Frontend

Выполните команду:

```bash
npm --version
node --version
```

Обе команды должны показать версии установленных программ.

---

## 🚀 Запуск проекта

Проект состоит из двух частей, которые нужно запускать **одновременно в разных терминалах**.

### Терминал 1: Запуск Backend

1. Откройте первый терминал
2. Перейдите в корень проекта:
   ```bash
   cd task-managment-system-api
   ```
3. Активируйте виртуальное окружение (если еще не активировано):
   - Windows: `venv\Scripts\activate`
   - macOS/Linux: `source venv/bin/activate`
4. Запустите сервер:
   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

✅ Вы должны увидеть сообщение: `Uvicorn running on http://0.0.0.0:8000`

**Оставьте этот терминал открытым!**

### Терминал 2: Запуск Frontend

1. Откройте второй терминал
2. Перейдите в папку frontend:
   ```bash
   cd task-managment-system-api/frontend
   ```
3. Запустите dev-сервер:
   ```bash
   npm run dev
   ```

✅ Вы должны увидеть сообщение с адресом, например: `Local: http://localhost:3000`

**Оставьте этот терминал открытым!**

---

## ✅ Проверка работы

### 1. Проверка Backend

Откройте в браузере: **http://localhost:8000/health**

Должен появиться ответ: `{"status": "ok"}`

### 2. Проверка Frontend

Откройте в браузере: **http://localhost:3000**

Должна открыться страница входа в систему.

### 3. Проверка документации API

Откройте в браузере: **http://localhost:8000/docs**

Должна открыться интерактивная документация Swagger UI.

---

## 🎯 Первые шаги

### Шаг 1: Регистрация первого пользователя

1. Откройте **http://localhost:3000/register**
2. Заполните форму регистрации:
   - Email: `admin@example.com`
   - Username: `admin`
   - Password: `admin123`
3. Нажмите "Зарегистрироваться"

### Шаг 2: Вход в систему

1. После регистрации вы автоматически войдете в систему
2. Или откройте **http://localhost:3000/login** и войдите с теми же данными

### Шаг 3: Создание первой доски

1. После входа нажмите кнопку "Создать доску"
2. Введите название доски
3. Нажмите "Создать"

### Шаг 4: Создание задачи

1. Откройте созданную доску
2. Нажмите "Создать задачу"
3. Заполните форму и сохраните

---

## 🐛 Решение проблем

### Проблема: "Python не найден" или "python: command not found"

**Решение:**
- Windows: Используйте `python` вместо `python3`
- macOS/Linux: Используйте `python3` вместо `python`
- Убедитесь, что Python установлен: `python --version` или `python3 --version`

### Проблема: "npm не найден" или "npm: command not found"

**Решение:**
1. Установите Node.js с официального сайта: https://nodejs.org/
2. После установки перезапустите терминал
3. Проверьте: `node --version` и `npm --version`

### Проблема: "ModuleNotFoundError" при запуске Backend

**Решение:**
1. Убедитесь, что виртуальное окружение активировано (должно быть `(venv)` в начале строки)
2. Переустановите зависимости: `pip install -r requirements.txt`

### Проблема: "Cannot find module" при запуске Frontend

**Решение:**
1. Убедитесь, что вы находитесь в папке `frontend`
2. Переустановите зависимости: `npm install`
3. Удалите папку `node_modules` и файл `package-lock.json`, затем снова выполните `npm install`

### Проблема: Backend не запускается, ошибка с портом 8000

**Решение:**
1. Проверьте, не запущен ли другой процесс на порту 8000
2. Измените порт в команде запуска:
   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
   ```
3. Если изменили порт, обновите URL в браузере на `http://localhost:8001`

### Проблема: Frontend не запускается, ошибка с портом 3000

**Решение:**
1. Проверьте, не запущен ли другой процесс на порту 3000
2. Vite автоматически предложит использовать другой порт (например, 3001)
3. Используйте адрес, который покажет Vite в терминале

### Проблема: "JWT_SECRET is required"

**Решение:**
1. Убедитесь, что файл `.env` создан в корне проекта
2. Проверьте, что в файле есть строка `JWT_SECRET=...`
3. Убедитесь, что файл называется именно `.env` (с точкой в начале)

### Проблема: База данных не создается

**Решение:**
1. База данных создается автоматически при первом запуске
2. Проверьте, что файл `app.db` появился в корне проекта
3. Если файла нет, проверьте права доступа к папке проекта

### Проблема: Страница не загружается или показывает ошибку

**Решение:**
1. Убедитесь, что оба сервера запущены (Backend и Frontend)
2. Проверьте консоль браузера (F12 → Console) на наличие ошибок
3. Очистите кэш браузера: `Ctrl+Shift+R` (Windows/Linux) или `Cmd+Shift+R` (Mac)
4. Попробуйте открыть в режиме инкогнито

### Проблема: Изменения в коде не отображаются

**Решение:**
1. Убедитесь, что серверы запущены с флагом `--reload` (Backend) и `npm run dev` (Frontend)
2. Сохраните файлы после изменений
3. Проверьте консоль терминалов на наличие ошибок
4. Перезапустите серверы

---

## 📝 Полезные команды

### Остановка серверов

В терминалах, где запущены серверы, нажмите: `Ctrl+C`

### Перезапуск Backend

```bash
# Остановите сервер (Ctrl+C)
# Затем снова запустите:
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Перезапуск Frontend

```bash
# Остановите сервер (Ctrl+C)
# Затем снова запустите:
npm run dev
```

### Деактивация виртуального окружения

Когда закончите работу, выполните:

```bash
deactivate
```

---

## 🎓 Дополнительная информация

### Важные адреса

После запуска проекта доступны следующие адреса:

- **Frontend (веб-интерфейс):** http://localhost:3000
- **Backend API:** http://localhost:8000
- **API Документация (Swagger):** http://localhost:8000/docs
- **API Документация (ReDoc):** http://localhost:8000/redoc
- **Health Check:** http://localhost:8000/health

### Тестовые пользователи

После заполнения базы данных тестовыми данными (см. раздел ниже) будут доступны:

- **Администратор:** admin@example.com / admin123
- **Пользователь:** user@example.com / user123
- **Гость:** guest@example.com / guest123

### Заполнение базы данных тестовыми данными (опционально)

Если хотите быстро заполнить базу данных тестовыми данными:

1. Убедитесь, что Backend запущен
2. В терминале (где запущен Backend) выполните:
   ```bash
   python fill_database.py
   ```
3. Это создаст несколько пользователей, досок и задач для тестирования

---

