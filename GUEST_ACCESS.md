# Гостевой доступ и публичные доски

## 📑 Содержание

- [Обзор](#-обзор)
- [Типы пользователей](#-типы-пользователей)
- [Регистрация](#-регистрация)
- [Работа с публичными досками](#-работа-с-публичными-досками)
- [Ограничения для гостей](#-ограничения-для-гостей)
- [Примеры использования](#-примеры-использования)
- [Таблица прав доступа](#-таблица-прав-доступа)

## 📋 Обзор

В систему добавлен функционал гостевого доступа и публичных досок:

- **Публичные доски** - доски, которые могут просматривать все авторизованные пользователи, включая гостей
- **Приватные доски** - доски, доступные только владельцу и приглашённым участникам
- **Роль "Гость"** - пользователь с ограниченными правами (только просмотр публичных досок)

## 👥 Типы пользователей

### 1. Администратор (admin)
- Полный доступ к системе
- Может просматривать всех пользователей
- Создаётся первым при регистрации

### 2. Обычный пользователь (user)
- Может создавать доски (публичные и приватные)
- Может создавать задачи на своих досках
- Может просматривать публичные доски других пользователей
- Может редактировать и удалять только свои доски

### 3. Гость (guest)
- **Может:** просматривать публичные доски и их задачи
- **Не может:** создавать, редактировать или удалять доски
- **Не может:** создавать, редактировать или удалять задачи
- **Не может:** просматривать приватные доски

## 🔐 Регистрация

### Регистрация администратора
```bash
POST /auth/register-admin
Content-Type: application/json

{
  "username": "admin",
  "email": "admin@example.com",
  "password": "admin123"
}
```
**Примечание:** Доступно только если в системе нет пользователей.

### Регистрация обычного пользователя
```bash
POST /auth/register
Content-Type: application/json

{
  "username": "user1",
  "email": "user1@example.com",
  "password": "user123"
}
```

### Регистрация гостя
```bash
POST /auth/register-guest
Content-Type: application/json

{
  "username": "guest1",
  "email": "guest1@example.com",
  "password": "guest123"
}
```

## 📋 Работа с публичными досками

### Создание публичной доски (обычный пользователь)
```bash
POST /boards/
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "Public Task Board",
  "description": "Everyone can view this board",
  "public": true  ← Делает доску публичной
}
```

### Создание приватной доски (обычный пользователь)
```bash
POST /boards/
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "Private Task Board",
  "description": "Only I can view this board",
  "public": false  ← Делает доску приватной (по умолчанию)
}
```

### Изменение статуса публичности доски
```bash
PUT /boards/{board_id}
Authorization: Bearer <token>
Content-Type: application/json

{
  "public": true  ← Делает приватную доску публичной
}
```

### Просмотр публичной доски (гость)
```bash
GET /boards/{board_id}
Authorization: Bearer <guest_token>
```

**Успешный ответ (200):**
```json
{
  "id": 1,
  "title": "Public Task Board",
  "description": "Everyone can view this board",
  "public": true,
  "created_by": 2,
  "created_at": "2025-11-21T10:00:00",
  "tasks": [
    {
      "id": 1,
      "title": "Task 1",
      "status": "todo",
      "priority": "high",
      ...
    }
  ]
}
```

### Попытка просмотра приватной доски (гость)
```bash
GET /boards/{board_id}
Authorization: Bearer <guest_token>
```

**Ответ с ошибкой (403):**
```json
{
  "detail": "This board is private. Guests can only view public boards"
}
```

## 📝 Работа с задачами на публичных досках

### Просмотр задач на публичной доске (гость)
```bash
GET /boards/{board_id}/tasks
Authorization: Bearer <guest_token>
```

**Успешный ответ (200):**
```json
[
  {
    "id": 1,
    "title": "Task 1",
    "description": "Description",
    "status": "todo",
    "priority": "high",
    "board_id": 1,
    "created_by": 2,
    "created_at": "2025-11-21T10:00:00",
    "updated_at": "2025-11-21T10:00:00"
  }
]
```

### Попытка просмотра задач на приватной доске (гость)
```bash
GET /boards/{board_id}/tasks
Authorization: Bearer <guest_token>
```

**Ответ с ошибкой (403):**
```json
{
  "detail": "This board is private. Guests can only view public boards"
}
```

## 🚫 Ограничения для гостей

Гости **НЕ МОГУТ**:

### 1. Создавать доски
```bash
POST /boards/
Authorization: Bearer <guest_token>
# → Вернёт ошибку (403)
```

### 2. Обновлять доски (даже публичные)
```bash
PUT /boards/{board_id}
Authorization: Bearer <guest_token>
# → Вернёт ошибку (403): "Guests can only view public boards"
```

### 3. Удалять доски (даже публичные)
```bash
DELETE /boards/{board_id}
Authorization: Bearer <guest_token>
# → Вернёт ошибку (403): "Guests can only view public boards"
```

### 4. Создавать задачи
```bash
POST /boards/{board_id}/tasks
Authorization: Bearer <guest_token>
# → Вернёт ошибку (403)
```

### 5. Обновлять задачи
```bash
PUT /boards/{board_id}/tasks/{task_id}
Authorization: Bearer <guest_token>
# → Вернёт ошибку (403)
```

### 6. Удалять задачи
```bash
DELETE /boards/{board_id}/tasks/{task_id}
Authorization: Bearer <guest_token>
# → Вернёт ошибку (403)
```

## 🔍 Примеры использования

### Сценарий 1: Просмотр публичных досок гостем

```bash
# 1. Регистрация гостя
curl -X POST http://localhost:8000/auth/register-guest \
  -H "Content-Type: application/json" \
  -d '{
    "username": "guest1",
    "email": "guest1@example.com",
    "password": "guest123"
  }'

# Ответ: { "access_token": "...", "token_type": "bearer" }

# 2. Просмотр публичной доски
curl -X GET http://localhost:8000/boards/1 \
  -H "Authorization: Bearer <guest_token>"

# Успех: Доска и задачи отображаются

# 3. Попытка просмотра приватной доски
curl -X GET http://localhost:8000/boards/2 \
  -H "Authorization: Bearer <guest_token>"

# Ошибка 403: "This board is private..."
```

### Сценарий 2: Создание публичной доски пользователем

```bash
# 1. Регистрация обычного пользователя
curl -X POST http://localhost:8000/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "user1",
    "email": "user1@example.com",
    "password": "user123"
  }'

# 2. Создание публичной доски
curl -X POST http://localhost:8000/boards/ \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "My Public Board",
    "description": "Everyone can see this",
    "public": true
  }'

# 3. Теперь гости могут просматривать эту доску
```

### Сценарий 3: Изменение приватной доски на публичную

```bash
# 1. Создание приватной доски
curl -X POST http://localhost:8000/boards/ \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Initially Private",
    "public": false
  }'

# 2. Изменение на публичную
curl -X PUT http://localhost:8000/boards/3 \
  -H "Authorization: Bearer <user_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "public": true
  }'

# 3. Теперь гости могут просматривать эту доску
```

## 🧪 Тестирование

Для тестирования гостевого доступа созданы специальные тесты в `tests/test_guests.py`:

```bash
# Запустить все тесты гостей
pytest tests/test_guests.py -v -s

# Запустить конкретный тест
pytest tests/test_guests.py::test_guest_can_view_public_board -v -s
```

### Покрытие тестами:
- ✅ Просмотр публичных досок гостем
- ✅ Запрет просмотра приватных досок гостем
- ✅ Просмотр задач на публичных досках гостем
- ✅ Запрет просмотра задач на приватных досках гостем
- ✅ Запрет создания досок гостем
- ✅ Запрет обновления досок гостем
- ✅ Запрет удаления досок гостем
- ✅ Создание публичных и приватных досок пользователем
- ✅ Изменение статуса публичности доски

## 📊 Таблица прав доступа

| Действие | Админ | Пользователь | Гость |
|----------|-------|--------------|-------|
| Просмотр публичных досок | ✅ | ✅ | ✅ |
| Просмотр приватных досок | ✅ | ✅ (своих) | ❌ |
| Создание досок | ✅ | ✅ | ❌ |
| Обновление досок | ✅ | ✅ (своих) | ❌ |
| Удаление досок | ✅ | ✅ (своих) | ❌ |
| Просмотр задач на публичных досках | ✅ | ✅ | ✅ |
| Просмотр задач на приватных досках | ✅ | ✅ (на своих) | ❌ |
| Создание задач | ✅ | ✅ | ❌ |
| Обновление задач | ✅ | ✅ | ❌ |
| Удаление задач | ✅ | ✅ | ❌ |

## 🛠 Миграция существующей БД

Если у вас уже есть база данных, выполните миграцию:

```bash
# 1. Остановите сервер
# 2. Запустите скрипт миграции
python migrate_add_public.py

# 3. Запустите сервер
python run.py
```

Скрипт добавит поле `public` в таблицу `boards` со значением по умолчанию `false`.

## 📝 Примечания

1. **Безопасность:** Гости могут просматривать ТОЛЬКО публичные доски
2. **По умолчанию:** Все новые доски создаются приватными (`public=false`)
3. **Владелец:** Только владелец доски может изменить её статус публичности
4. **Задачи:** Задачи наследуют уровень доступа от доски

## 🔗 Связанные документы

- [README.md](README.md) - Основная документация
- [EXAMPLES.md](EXAMPLES.md) - Примеры использования API
- [tests/README.md](tests/README.md) - Документация по тестам

