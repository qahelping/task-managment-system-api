# Руководство по внесению вклада

Спасибо за интерес к проекту Task Management System API!

## 🎯 Цель проекта

Это учебный проект, созданный для изучения:
- FastAPI и REST API разработки
- SQLAlchemy ORM
- JWT аутентификации
- Pytest тестирования
- Pydantic валидации

## 🛠 Процесс разработки

### 1. Настройка окружения

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd task-managment-system-api

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env
```

### 2. Создание новой функции

1. Создайте новую ветку:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. Внесите изменения

3. Запустите тесты:
   ```bash
   pytest
   ```

4. Убедитесь, что код соответствует стандартам:
   ```bash
   # Форматирование кода (опционально)
   black app/
   
   # Проверка типов (опционально)
   mypy app/
   ```

### 3. Отправка изменений

1. Зафиксируйте изменения:
   ```bash
   git add .
   git commit -m "feat: краткое описание изменений"
   ```

2. Отправьте ветку:
   ```bash
   git push origin feature/your-feature-name
   ```

3. Создайте Pull Request

## 📝 Стиль кода

### Python

- Используйте PEP 8
- Максимальная длина строки: 120 символов
- Используйте type hints
- Добавляйте docstrings для функций и классов

Пример:

```python
def create_board(db: Session, board_data: BoardCreate, user_id: int) -> Board:
    """
    Создать новую доску.
    
    Args:
        db: Сессия базы данных
        board_data: Данные для создания доски
        user_id: ID пользователя-создателя
        
    Returns:
        Board: Созданная доска
    """
    # ... код ...
```

### Commit Messages

Используйте Conventional Commits:

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `test:` - добавление/изменение тестов
- `refactor:` - рефакторинг кода
- `style:` - форматирование, пробелы и т.д.
- `chore:` - обновление зависимостей и т.д.

Примеры:
```
feat: add task filtering by assignee
fix: resolve JWT token expiration issue
docs: update README with deployment instructions
test: add tests for board deletion cascade
```

## 🧪 Тестирование

### Написание тестов

- Все новые функции должны иметь тесты
- Используйте pytest fixtures
- Тесты должны быть изолированными
- Покрытие кода должно быть > 80%

Пример:

```python
def test_create_board(client, auth_headers):
    """Тест создания доски"""
    response = client.post(
        "/boards/",
        json={"title": "Test Board"},
        headers=auth_headers
    )
    
    assert response.status_code == 201
    assert response.json()["title"] == "Test Board"
```

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=app --cov-report=html

# Конкретный файл
pytest tests/test_boards.py

# Конкретный тест
pytest tests/test_boards.py::test_create_board
```

## 📚 Документация

### API Documentation

- Используйте docstrings в router функциях
- Добавляйте описания к Pydantic схемам
- Обновляйте README.md при изменении API

### Code Documentation

```python
@router.post("/boards/", response_model=BoardResponse)
def create_board(
    board_data: BoardCreate,
    db: Session = Depends(get_db),
    current_user_id: int = Depends(get_current_user_id)
):
    """
    Создать новую доску.
    
    Требуется аутентификация.
    Доска будет создана от имени текущего пользователя.
    """
    # ... код ...
```

## 🐛 Сообщение об ошибках

При создании Issue включите:

1. **Описание проблемы**
2. **Шаги для воспроизведения**
3. **Ожидаемое поведение**
4. **Актуальное поведение**
5. **Версию Python**
6. **Логи ошибок**

Пример:

```markdown
## Описание
JWT токен не обновляется после истечения

## Шаги для воспроизведения
1. Получить токен через /auth/login
2. Подождать 24 часа
3. Попытаться использовать токен

## Ожидаемое поведение
Токен должен автоматически обновиться

## Актуальное поведение
Получаю ошибку 401 Unauthorized

## Окружение
- Python 3.11
- FastAPI 0.104.1
```

## ✨ Предложения по улучшению

Идеи для улучшения проекта:

- [ ] Добавить role-based access control (RBAC)
- [ ] Реализовать websocket уведомления
- [ ] Добавить файловые вложения к задачам
- [ ] Создать систему комментариев
- [ ] Добавить поиск по задачам
- [ ] Реализовать экспорт данных (JSON, CSV)
- [ ] Добавить Docker поддержку
- [ ] Создать CI/CD pipeline

## 📞 Контакты

Если у вас есть вопросы, создайте Issue в репозитории.

## 📄 Лицензия

Проект распространяется под MIT License. См. [LICENSE](LICENSE) файл.

---

**Спасибо за ваш вклад! 🙏**

