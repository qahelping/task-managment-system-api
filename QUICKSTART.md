# 🚀 Быстрый старт

Это краткое руководство для быстрого запуска проекта.

## Минимальные требования

- Python 3.10 или выше
- pip (устанавливается вместе с Python)

## Быстрая установка (5 минут)

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows

# Установите пакеты
pip install -r requirements.txt
```

### 2. Создайте файл .env

Создайте файл `.env` в корне проекта:

```env
DATABASE_URL=sqlite:///./app.db
JWT_SECRET=my-super-secret-key-change-this-in-production-32chars
JWT_EXPIRE_MINUTES=1440
```

### 3. Запустите сервер

```bash
uvicorn app.main:app --reload
```

✅ Сервер запущен на http://localhost:8000

## Первые шаги

### Шаг 1: Откройте документацию

Откройте в браузере: **http://localhost:8000/docs**

### Шаг 2: Зарегистрируйте администратора

В Swagger UI найдите эндпоинт `POST /auth/register-admin` и выполните:

```json
{
  "username": "admin",
  "email": "admin@example.com",
  "password": "admin123"
}
```

**Сохраните полученный токен!**

### Шаг 3: Авторизуйтесь в Swagger UI

1. Нажмите кнопку **"Authorize"** в правом верхнем углу
2. Введите: `Bearer <ваш_токен>`
3. Нажмите "Authorize"

Теперь вы можете использовать все защищённые эндпоинты!

### Шаг 4: Создайте доску

Используйте эндпоинт `POST /boards/`:

```json
{
  "title": "Моя первая доска",
  "description": "Тестовая доска"
}
```

### Шаг 5: Создайте задачу

Используйте эндпоинт `POST /boards/{board_id}/tasks`:

```json
{
  "title": "Моя первая задача",
  "description": "Описание задачи",
  "status": "todo",
  "priority": "high"
}
```

## Готово! 🎉

Вы успешно запустили Task Management System API!

## Что дальше?

- 📖 Прочитайте полную документацию в [README.md](README.md)
- 🧪 Запустите тесты: `pytest`
- 📬 Попробуйте Postman коллекцию: [POSTMAN.md](POSTMAN.md)

## Частые проблемы

### Не могу импортировать модули

Убедитесь, что вы:
1. Активировали виртуальное окружение
2. Установили все зависимости: `pip install -r requirements.txt`

### Ошибка "JWT_SECRET is required"

Создайте файл `.env` с переменной `JWT_SECRET`.

### База данных не создаётся

База данных создаётся автоматически при первом запуске. Файл `app.db` должен появиться в корне проекта.

## Остановка сервера

Нажмите `Ctrl+C` в терминале, где запущен сервер.

## Команды для копирования

```bash
# Полная последовательность команд для запуска
python -m venv venv
source venv/bin/activate  # macOS/Linux
pip install -r requirements.txt
# Создайте .env файл вручную
uvicorn app.main:app --reload
```

---

**Нужна помощь?** Смотрите полную документацию в [README.md](README.md)

