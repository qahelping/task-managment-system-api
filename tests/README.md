
# Автотесты API

Простые автотесты для Task Management System API с использованием библиотеки `requests`.

## ✨ Новое: Три типа пользователей

- **Админ** - используется для тестов аутентификации и управления пользователями
- **Обычные пользователи** - создают доски и задачи (каждый тест создаёт нового пользователя)
- **Гости** - могут только просматривать публичные доски и задачи (ограниченные права) ⭐

## 🚀 Запуск тестов (пошагово)

### Шаг 1: Остановите сервер
Если сервер запущен, остановите его: `Ctrl+C`

### Шаг 2: Очистите базу данных

```bash
# Способ 1: Используя скрипт
./clean_db.sh

# Способ 2: Вручную
rm -f test.db app.db task_management.db
```

### Шаг 3: Запустите сервер

```bash
python run.py
```

Дождитесь сообщения:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
```

### Шаг 4: Запустите тесты (в ДРУГОМ терминале)

```bash
# Все тесты с выводом
pytest tests/ -v -s

# Только один файл
pytest tests/test_boards.py -v -s

# Конкретный тест
pytest tests/test_boards.py::test_create_board -v -s
```

## 📋 Быстрый старт (автоматический)

```bash
# Автоматически очистит БД, запустит сервер, выполнит тесты
./run_tests.sh
```

## 📁 Структура тестов

```
tests/
├── clean_db.sh          # Скрипт очистки БД
├── conftest.py          # Конфигурация pytest
├── helpers.py           # Вспомогательные функции
├── test_auth.py         # Тесты аутентификации (админ)
├── test_users.py        # Тесты пользователей (админ)
├── test_boards.py       # Тесты досок (обычные пользователи) ⭐
├── test_tasks.py        # Тесты задач (обычные пользователи) ⭐
└── test_guests.py       # Тесты гостевого доступа (публичные доски) 🆕
```

**Всего: 45+ автотестов**

## 🎯 Что изменилось

### 1. Добавлены эндпоинты регистрации пользователей

**Обычный пользователь:**
```python
POST /auth/register
{
    "username": "user1",
    "email": "user1@example.com",
    "password": "user123"
}
```

**Гость (ограниченные права):**
```python
POST /auth/register-guest
{
    "username": "guest1",
    "email": "guest1@example.com",
    "password": "guest123"
}
```

### 2. Тесты теперь используют разные роли

#### Тесты с АДМИНОМ (test_auth.py, test_users.py):
```python
from tests.helpers import get_admin_token

def test_get_users():
    token = get_admin_token()  # Токен админа
    response = requests.get(f"{BASE_URL}/users/", headers=...)
```

#### Тесты с ОБЫЧНЫМИ ПОЛЬЗОВАТЕЛЯМИ (test_boards.py, test_tasks.py):
```python
from tests.helpers import get_user_token

def test_create_board():
    token = get_user_token()  # Токен обычного пользователя
    response = requests.post(f"{BASE_URL}/boards/", json=..., headers=...)
```

#### Тесты с ГОСТЯМИ (test_guests.py):
```python
from tests.helpers import get_guest_token, get_user_token

def test_guest_can_view_public_board():
    # Создаём публичную доску пользователем
    user_token = get_user_token()
    board = create_board(title="Public Board", public=True, token=user_token)
    
    # Гость просматривает публичную доску
    guest_token = get_guest_token()
    response = requests.get(f"{BASE_URL}/boards/{board_id}", 
                           headers=get_auth_headers(guest_token))
    assert response.status_code == 200  # Успех!
```

## 💡 Вспомогательные функции (helpers.py)

### Для админа:
```python
# Получить токен админа (один для всех тестов)
admin_token = get_admin_token()
```

### Для обычных пользователей:
```python
# Получить токен нового пользователя (создаётся автоматически)
user_token = get_user_token()

# Создать доску от имени пользователя (может быть публичной!)
response = create_board(title="My Board", public=True, token=user_token)

# Создать задачу от имени пользователя
response = create_task(
    board_id=1,
    title="My Task",
    status="todo",
    priority="high",
    token=user_token
)
```

### Для гостей:
```python
# Получить токен нового гостя (создаётся автоматически)
guest_token = get_guest_token()

# Гость может просматривать ТОЛЬКО публичные доски
# Приватные доски вернут 403 ошибку
```

### Универсальные:
```python
# Получить заголовки авторизации
headers = get_auth_headers(token)
```

## 📝 Примеры тестов

### Тест с админом:

```python
import requests
from tests.conftest import BASE_URL
from tests.helpers import get_admin_token, get_auth_headers

def test_get_users():
    """Получение списка пользователей (только админ)"""
    # Получаем токен АДМИНА
    token = get_admin_token()
    assert token is not None
    
    # Делаем запрос
    url = f"{BASE_URL}/users/"
    headers = get_auth_headers(token)
    response = requests.get(url, headers=headers)
    
    assert response.status_code == 200
    print(f"✓ Users: {response.json()}")
```

### Тест с обычным пользователем:

```python
import requests
from tests.conftest import BASE_URL
from tests.helpers import get_user_token, get_auth_headers

def test_create_board():
    """Создание доски обычным пользователем"""
    # Получаем токен НОВОГО ПОЛЬЗОВАТЕЛЯ
    token = get_user_token()
    assert token is not None
    
    # Создаём доску
    url = f"{BASE_URL}/boards/"
    headers = get_auth_headers(token)
    payload = {
        "title": "My Board",
        "description": "Created by regular user"
    }
    
    response = requests.post(url, json=payload, headers=headers)
    
    assert response.status_code == 201
    print(f"✓ Board created: {response.json()}")
```

## 🔧 Как это работает

### При запуске test_auth.py или test_users.py:
1. Создаётся один админ: `admin@example.com`
2. Его токен кэшируется
3. Все тесты используют этот же токен админа

### При запуске test_boards.py или test_tasks.py:
1. Каждый тест создаёт НОВОГО обычного пользователя
2. Пользователь получает уникальный email: `user1_1234567890@example.com`
3. Этот пользователь создаёт свои доски и задачи
4. Тесты изолированы друг от друга

## 📊 Полезные команды

```bash
# Запуск всех тестов
pytest tests/ -v -s

# Только тесты админа
pytest tests/test_auth.py tests/test_users.py -v -s

# Только тесты пользователей (доски и задачи)
pytest tests/test_boards.py tests/test_tasks.py -v -s

# С остановкой на первой ошибке
pytest tests/ -x -v -s

# Запуск только упавших тестов
pytest tests/ --lf -v -s
```

## 🐛 Решение проблем

### ❌ FAILED - assert 403 == 201

**Причина:** Админ уже зарегистрирован в БД

**Решение:**
```bash
# 1. Остановите сервер (Ctrl+C)
# 2. Очистите БД
./clean_db.sh
# 3. Запустите сервер
python run.py
# 4. Запустите тесты
pytest tests/ -v -s
```

### ❌ AssertionError: Не удалось получить токен

**Причина:** Сервер недоступен

**Решение:**
```bash
# Проверьте что сервер работает
curl http://127.0.0.1:8000/health

# Если не работает - запустите:
python run.py
```

### ❌ Connection refused

**Решение:** Сервер не запущен
```bash
python run.py
```

## 🎯 Покрытие тестами

### Аутентификация (админ) ✅
- Health check
- Регистрация админа
- Дубликат админа (403)
- Логин админа
- Неправильный email/пароль
- Доступ без токена/с невалидным токеном

### Пользователи (админ) ✅
- Получение списка пользователей
- Получение по ID
- Пользователь не найден
- Доступ без авторизации

### Доски (обычные пользователи) ✅
- Создание доски пользователем
- Получение списка досок пользователя
- Получение, обновление, удаление
- Обработка несуществующих досок

### Задачи (обычные пользователи) ✅
- Создание задачи пользователем
- Получение списка задач
- Фильтрация по статусу и приоритету
- Получение, обновление, удаление
- Обработка несуществующих задач

### Гостевой доступ (гости) ✅ 🆕
- Просмотр публичных досок гостем
- Запрет просмотра приватных досок
- Просмотр задач на публичных досках
- Запрет просмотра задач на приватных досках
- Запрет создания/изменения/удаления досок гостем
- Создание публичных и приватных досок пользователем
- Изменение статуса публичности доски

## 🆕 Новые API эндпоинты

### Регистрация обычного пользователя

```bash
POST /auth/register
Content-Type: application/json

{
  "username": "user1",
  "email": "user1@example.com",
  "password": "user123"
}
```

### Регистрация гостя 🆕

```bash
POST /auth/register-guest
Content-Type: application/json

{
  "username": "guest1",
  "email": "guest1@example.com",
  "password": "guest123"
}
```

**Ответ (для обоих):**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs...",
  "token_type": "bearer"
}
```

### Создание публичной доски 🆕

```bash
POST /boards/
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "My Public Board",
  "description": "Everyone can see this",
  "public": true  ← Делает доску публичной
}
```

### Проверка роли пользователя

```bash
GET /users/1
Authorization: Bearer <token>
```

**Ответ:**
```json
{
  "id": 1,
  "username": "user1",
  "email": "user1@example.com",
  "role": "user",  ← Может быть: "admin", "user", "guest"
  "created_at": "2025-11-21T10:30:00"
}
```

## 📚 Дополнительная информация

- [Документация API](../README.md)
- [Гостевой доступ и публичные доски](../GUEST_ACCESS.md) 🆕
- [Примеры использования](../EXAMPLES.md)
- [Структура проекта](../PROJECT_STRUCTURE.md)

## 🎯 Что нового

### Публичные доски 🆕
- Доски теперь имеют поле `public` (true/false)
- Публичные доски может просматривать любой авторизованный пользователь
- Приватные доски доступны только владельцу

### Роль "Гость" 🆕
- Новая роль с ограниченными правами
- Может просматривать ТОЛЬКО публичные доски и их задачи
- Не может создавать, изменять или удалять что-либо

### Новые тесты 🆕
- `test_guests.py` - 10+ тестов гостевого доступа
- Проверка прав доступа к публичным и приватным доскам
- Проверка ограничений для гостей

Удачного тестирования! 🎉
