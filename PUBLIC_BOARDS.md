# Доступ к публичным доскам

## 📑 Содержание

- [Текущая реализация](#-текущая-реализация)
- [Как получить доступ](#-как-получить-доступ-к-публичной-доске)
- [Создание публичной доски](#-создание-публичной-доски)
- [Ограничения доступа](#-ограничения-доступа)
- [Планы на будущее](#-планы-на-будущее)

## Текущая реализация

В текущей версии API **все эндпоинты требуют аутентификации**, включая просмотр публичных досок. Это означает:

1. **Для просмотра публичной доски нужно быть авторизованным**
2. Публичные доски доступны всем авторизованным пользователям
3. Приватные доски доступны только владельцу и администраторам

## Как получить доступ к публичной доске

### Вариант 1: Через фронтенд (рекомендуется)

1. **Войдите в систему**:
   - Откройте `http://localhost:3000/login`
   - Введите email и пароль
   - Или зарегистрируйтесь как новый пользователь

2. **Найдите публичную доску**:
   - Перейдите на страницу `/boards` - там отображаются все доступные вам доски
   - Публичные доски отмечены значком "Публичная"
   - Или перейдите напрямую по ссылке `/boards/{id}` если знаете ID доски

3. **Просмотр доски**:
   - Кликните на доску для просмотра
   - Вы увидите Kanban-доску с задачами

### Вариант 2: Через API

1. **Получите токен** (войдите в систему):
```bash
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email": "your@email.com", "password": "yourpassword"}'
```

2. **Получите список досок**:
```bash
curl -X GET "http://localhost:8000/boards/" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

3. **Найдите публичную доску** (в ответе будет поле `"public": true`)

4. **Просмотрите публичную доску**:
```bash
curl -X GET "http://localhost:8000/boards/{board_id}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Вариант 3: Как гость (guest)

1. **Зарегистрируйтесь как гость**:
```bash
curl -X POST "http://localhost:8000/auth/register-guest" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "guest_user",
    "email": "guest@example.com",
    "password": "guest123"
  }'
```

2. **Войдите как гость** и просматривайте только публичные доски

## Создание публичной доски

Чтобы создать публичную доску:

1. **Через фронтенд**:
   - Войдите в систему
   - Нажмите "Создать доску"
   - Отметьте чекбокс "Публичная доска"
   - Сохраните

2. **Через API**:
```bash
curl -X POST "http://localhost:8000/boards/" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Моя публичная доска",
    "description": "Описание доски",
    "public": true
  }'
```

## Ограничения доступа

- **Гости (guest)**: могут только просматривать публичные доски, не могут создавать или редактировать
- **Обычные пользователи (user)**: могут просматривать все публичные доски и свои приватные доски
- **Администраторы (admin)**: имеют полный доступ ко всем доскам

## Планы на будущее

В будущих версиях может быть добавлен эндпоинт для просмотра публичных досок без авторизации:
- `GET /boards/public` - список всех публичных досок
- `GET /boards/public/{board_id}` - просмотр публичной доски без токена



