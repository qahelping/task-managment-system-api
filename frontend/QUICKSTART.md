# Быстрый старт - Frontend

## 📑 Содержание

- [Предварительные требования](#-предварительные-требования)
- [Установка и запуск](#-установка-и-запуск)
- [Первый вход](#-первый-вход)
- [Основные функции](#-основные-функции)
- [Структура проекта](#-структура-проекта)

## Предварительные требования

1. Node.js версии 18 или выше
2. npm или yarn
3. Запущенный API сервер на `http://localhost:8000`

## Установка и запуск

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env`:
```bash
cp .env.example .env
```

Или создайте `.env` вручную:
```env
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_NAME=Task Management
```

4. Запустите сервер разработки:
```bash
npm run dev
```

5. Откройте браузер и перейдите на `http://localhost:3000`

## Первый вход

1. Если в системе нет пользователей, зарегистрируйте первого администратора через API:
   - POST `/auth/register-admin` с данными первого админа

2. Или зарегистрируйтесь как обычный пользователь через форму регистрации

3. Войдите в систему используя email и пароль

## Основные функции

- **Регистрация/Вход**: `/login` и `/register`
- **Главная страница**: `/` - Dashboard со статистикой
- **Список досок**: `/boards` - все доски пользователя
- **Детали доски**: `/boards/:id` - Kanban-доска с задачами
- **Админ-панель**: `/admin` - управление пользователями (только для админов)

## Структура проекта

- `src/components/ui/` - базовые UI компоненты
- `src/pages/` - страницы приложения
- `src/services/` - сервисы для работы с API
- `src/stores/` - Zustand stores для управления состоянием
- `src/types/` - TypeScript типы

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в `dist/`



