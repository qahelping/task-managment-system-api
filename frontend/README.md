# Task Management Board - Frontend

Веб-приложение для управления задачами (аналог Trello) с Kanban-досками.

## 📑 Содержание

- [Технологический стек](#-технологический-стек)
- [Установка](#-установка)
- [Сборка для продакшена](#-сборка-для-продакшена)
- [Структура проекта](#-структура-проекта)
- [Основные функции](#-основные-функции)
- [API Интеграция](#-api-интеграция)
- [Разработка](#-разработка)
- [Особенности реализации](#-особенности-реализации)

## Технологический стек

- **React 18** с TypeScript
- **Vite** - сборщик
- **Tailwind CSS** - стилизация
- **React Router DOM** - маршрутизация
- **Zustand** - управление состоянием
- **Axios** - HTTP-клиент
- **@dnd-kit** - drag & drop для Kanban-досок
- **React Icons** - иконки
- **date-fns** - работа с датами

## Установка

1. Установите зависимости:

```bash
npm install
```

2. Создайте файл `.env` в корне проекта `frontend/`:

```env
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_NAME=Task Management
```

3. Запустите сервер разработки:

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в директории `dist/`

Для предпросмотра продакшен-сборки:

```bash
npm run preview
```

## Структура проекта

```
src/
├── components/          # React компоненты
│   ├── ui/             # Базовые UI компоненты
│   ├── layout/         # Компоненты макета
│   ├── kanban/         # Kanban-доска компоненты
│   ├── modals/         # Модальные окна
│   └── routes/         # Компоненты маршрутизации
├── pages/              # Страницы приложения
│   ├── auth/           # Страницы аутентификации
│   ├── boards/         # Страницы досок
│   └── admin/          # Административная панель
├── services/           # Сервисы для работы с API
├── stores/             # Zustand stores
├── types/              # TypeScript типы
├── utils/              # Утилиты
└── main.tsx            # Точка входа
```

## Основные функции

### Аутентификация
- Регистрация пользователей
- Вход в систему
- Защищенные маршруты
- Автоматический logout при 401 ошибке

### Управление досками
- Создание, редактирование, удаление досок
- Список всех досок
- Поиск по названию
- Публичные/приватные доски

### Управление задачами
- Создание, редактирование, удаление задач
- Kanban-доска с drag & drop
- Фильтрация по статусу и приоритету
- Поиск по задачам
- Изменение статуса перетаскиванием

### Административная панель
- Просмотр всех пользователей
- Поиск пользователей
- Управление ролями (если поддерживается API)

## API Интеграция

Приложение работает с REST API Task Management System. Убедитесь, что API сервер запущен на `http://localhost:8000` (или измените `VITE_API_BASE_URL` в `.env`).

### Эндпоинты

- `/auth/login` - вход
- `/auth/register` - регистрация
- `/boards/` - список досок
- `/boards/{id}` - детали доски
- `/boards/{id}/tasks` - задачи доски
- `/users/` - список пользователей
- `/users/me` - текущий пользователь

## Разработка

### Запуск в режиме разработки

```bash
npm run dev
```

### Линтинг

```bash
npm run lint
```

### Проверка типов

```bash
npx tsc --noEmit
```

## Особенности реализации

- **Приватные маршруты**: защита страниц требует аутентификации
- **Админские маршруты**: доступ только для пользователей с ролью `admin`
- **Глобальное состояние**: Zustand stores для auth, boards, tasks, UI
- **Обработка ошибок**: централизованная обработка через interceptors
- **Уведомления**: Toast-уведомления для успешных операций и ошибок
- **Адаптивный дизайн**: поддержка mobile, tablet, desktop
- **Drag & Drop**: реализовано с помощью @dnd-kit

## Переменные окружения

- `VITE_API_BASE_URL` - базовый URL API (по умолчанию: http://localhost:8000)
- `VITE_APP_NAME` - название приложения

## Лицензия

MIT



