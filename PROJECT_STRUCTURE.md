# Структура проекта Task Management System API

## 📑 Содержание

- [Полная структура директорий](#-полная-структура-директорий)
- [Описание основных компонентов](#-описание-основных-компонентов)
- [Поток данных в приложении](#-поток-данных-в-приложении)
- [Схема базы данных](#-схема-базы-данных)
- [Статистика проекта](#-статистика-проекта)
- [Основные паттерны проектирования](#-основные-паттерны-проектирования)

## 📂 Полная структура директорий

```
task-managment-system-api/
│
├── app/                                # Основной код приложения
│   ├── __init__.py
│   ├── main.py                        # Точка входа FastAPI приложения
│   ├── database.py                    # Конфигурация базы данных
│   │
│   ├── core/                          # Ядро системы
│   │   ├── __init__.py
│   │   ├── config.py                  # Настройки из .env
│   │   └── security.py                # JWT и хэширование паролей
│   │
│   ├── models/                        # SQLAlchemy модели (БД)
│   │   ├── __init__.py
│   │   ├── user.py                    # Модель пользователя
│   │   ├── board.py                   # Модель доски
│   │   ├── task.py                    # Модель задачи
│   │   └── board_member.py            # Модель участника доски
│   │
│   ├── schemas/                       # Pydantic схемы (валидация)
│   │   ├── __init__.py
│   │   ├── user.py                    # Схемы пользователя
│   │   ├── board.py                   # Схемы доски
│   │   ├── task.py                    # Схемы задачи
│   │   └── auth.py                    # Схемы аутентификации
│   │
│   ├── routers/                       # API endpoints (маршруты)
│   │   ├── __init__.py
│   │   ├── auth.py                    # /auth/* - аутентификация
│   │   ├── users.py                   # /users/* - пользователи
│   │   ├── boards.py                  # /boards/* - доски
│   │   └── tasks.py                   # /boards/{id}/tasks/* - задачи
│   │
│   ├── services/                      # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── user_service.py            # Операции с пользователями
│   │   ├── board_service.py           # Операции с досками
│   │   └── task_service.py            # Операции с задачами
│   │
│   └── utils/                         # Утилиты (резерв)
│       └── __init__.py
│
├── tests/                             # Тесты pytest
│   ├── __init__.py
│   ├── conftest.py                    # Фикстуры для тестов
│   ├── helpers.py                     # Вспомогательные функции
│   ├── test_auth.py                   # Тесты аутентификации
│   ├── test_users.py                  # Тесты пользователей
│   ├── test_boards.py                 # Тесты досок
│   └── test_tasks.py                  # Тесты задач
│
├── .env                               # Переменные окружения (создать!)
├── .gitignore                         # Git исключения
├── requirements.txt                   # Python зависимости
├── pytest.ini                         # Конфигурация pytest
├── run.py                             # Скрипт запуска сервера
│
├── README.md                          # Основная документация
├── QUICKSTART.md                      # Быстрый старт
├── EXAMPLES.md                        # Примеры API запросов
├── POSTMAN.md                         # Postman коллекция
├── CONTRIBUTING.md                    # Руководство для разработчиков
├── LICENSE                            # MIT лицензия
└── PROJECT_STRUCTURE.md               # Этот файл

Генерируются автоматически:
├── app.db                             # SQLite база данных
└── test.db                            # Тестовая база данных
```

## 📋 Описание основных компонентов

### 1. app/main.py
**Назначение**: Главный файл приложения, точка входа.

**Содержит**:
- Создание FastAPI приложения
- Настройка CORS
- Регистрация роутеров
- Health check endpoint
- Инициализация БД при запуске

**Endpoints**:
- `GET /` - корневой эндпоинт
- `GET /health` - проверка здоровья API

### 2. app/database.py
**Назначение**: Конфигурация подключения к базе данных.

**Содержит**:
- Создание SQLAlchemy engine
- Создание сессии БД
- Базовый класс для моделей
- Функция `get_db()` для dependency injection
- Функция `init_db()` для создания таблиц

### 3. app/core/config.py
**Назначение**: Загрузка и хранение настроек приложения.

**Переменные**:
- `DATABASE_URL` - путь к SQLite базе
- `JWT_SECRET` - секретный ключ для JWT
- `JWT_EXPIRE_MINUTES` - время жизни токена
- `ADMIN_EMAIL`, `ADMIN_PASSWORD` - опциональные

### 4. app/core/security.py
**Назначение**: Безопасность, JWT, хэширование паролей.

**Функции**:
- `verify_password()` - проверка пароля
- `get_password_hash()` - хэширование пароля
- `create_access_token()` - создание JWT токена
- `decode_access_token()` - декодирование JWT
- `get_current_user_id()` - получение ID из токена

### 5. app/models/
**Назначение**: SQLAlchemy ORM модели (структура БД).

**Файлы**:
- `user.py` - таблица users (id, username, email, password_hash, role, created_at)
- `board.py` - таблица boards (id, title, description, created_by, created_at)
- `task.py` - таблица tasks (id, title, description, status, priority, board_id, created_by, created_at, updated_at)
- `board_member.py` - таблица board_members (id, board_id, user_id)

### 6. app/schemas/
**Назначение**: Pydantic схемы для валидации данных.

**Типы схем**:
- `*Create` - для создания объектов
- `*Update` - для обновления объектов
- `*Response` - для ответов API

**Файлы**:
- `user.py` - UserCreate, UserResponse
- `board.py` - BoardCreate, BoardUpdate, BoardResponse, BoardWithTasks
- `task.py` - TaskCreate, TaskUpdate, TaskResponse
- `auth.py` - LoginRequest, TokenResponse, RegisterAdminRequest

### 7. app/routers/
**Назначение**: API endpoints (маршруты HTTP запросов).

**Файлы**:
- `auth.py` - регистрация и логин
- `users.py` - CRUD пользователей
- `boards.py` - CRUD досок
- `tasks.py` - CRUD задач

### 8. app/services/
**Назначение**: Бизнес-логика приложения.

**Файлы**:
- `user_service.py` - операции с пользователями
- `board_service.py` - операции с досками
- `task_service.py` - операции с задачами

**Принцип**: Отделение бизнес-логики от HTTP handlers.

### 9. tests/
**Назначение**: Автоматические тесты pytest.

**Файлы**:
- `conftest.py` - фикстуры (client, admin_token, auth_headers)
- `test_auth.py` - тесты аутентификации (15 тестов)
- `test_users.py` - тесты пользователей (4 теста)
- `test_boards.py` - тесты досок (8 тестов)
- `test_tasks.py` - тесты задач (15+ тестов)

## 🔄 Поток данных в приложении

```
1. HTTP Request → Router (app/routers/*.py)
2. Router → Service (app/services/*.py)
3. Service → Model (app/models/*.py)
4. Model → Database (SQLite via SQLAlchemy)
5. Database → Model → Service → Router
6. Router → HTTP Response (Pydantic schema)
```

## 🗄️ Схема базы данных

```
┌─────────────────┐
│     users       │
├─────────────────┤
│ id (PK)         │
│ username        │───┐
│ email           │   │
│ password_hash   │   │
│ role            │   │
│ created_at      │   │
└─────────────────┘   │
                      │
                      │ created_by (FK)
                      │
                      ↓
┌─────────────────┐  ┌──────────────────┐
│     boards      │  │  board_members   │
├─────────────────┤  ├──────────────────┤
│ id (PK)         │←─│ board_id (FK)    │
│ title           │  │ user_id (FK)     │
│ description     │  └──────────────────┘
│ created_by (FK) │
│ created_at      │
└─────────────────┘
         │
         │ board_id (FK)
         ↓
┌─────────────────┐
│      tasks      │
├─────────────────┤
│ id (PK)         │
│ title           │
│ description     │
│ status          │
│ priority        │
│ board_id (FK)   │
│ created_by (FK) │
│ created_at      │
│ updated_at      │
└─────────────────┘
```

## 📊 Статистика проекта

### Количество файлов
- Python файлов: 28
- Документации: 7
- Конфигурации: 3
- **Всего**: 38+ файлов

### Количество кода
- Модели: ~150 строк
- Схемы: ~100 строк
- Сервисы: ~200 строк
- Роутеры: ~250 строк
- Тесты: ~500 строк
- **Всего**: ~1200+ строк кода

### API Endpoints
- Аутентификация: 2 эндпоинта
- Пользователи: 2 эндпоинта
- Доски: 5 эндпоинтов
- Задачи: 5 эндпоинтов
- Health: 1 эндпоинт
- **Всего**: 15 эндпоинтов

### Тесты
- test_auth.py: 8 тестов
- test_users.py: 4 теста
- test_boards.py: 8 тестов
- test_tasks.py: 17 тестов
- **Всего**: 37+ тестов

## 🎯 Основные паттерны проектирования

### 1. Layered Architecture (Слоистая архитектура)
```
Presentation Layer (Routers) 
    ↓
Business Logic Layer (Services)
    ↓
Data Access Layer (Models + Database)
```

### 2. Dependency Injection
- FastAPI Depends для инъекции зависимостей
- `get_db()` - инъекция сессии БД
- `get_current_user_id()` - инъекция аутентификации

### 3. Repository Pattern (частично)
- Services служат как repositories
- Инкапсуляция доступа к данным

### 4. DTO Pattern
- Pydantic схемы как Data Transfer Objects
- Разделение на Create/Update/Response

### 5. Middleware
- CORS middleware для кросс-доменных запросов
- JWT Bearer middleware для аутентификации

## 🔧 Конфигурационные файлы

### requirements.txt
Зависимости Python проекта:
- FastAPI - веб-фреймворк
- SQLAlchemy - ORM
- Pydantic - валидация
- PyJWT - JWT токены
- Passlib - хэширование
- pytest - тестирование

### pytest.ini
Конфигурация тестов:
- Путь к тестам
- Паттерны файлов
- Опции запуска

### .env
Переменные окружения (не в репозитории):
- Настройки БД
- JWT секреты
- Параметры приложения

### .gitignore
Исключения для Git:
- `__pycache__/`
- `*.db` (база данных)
- `.env` (секреты)
- `venv/` (виртуальное окружение)

## 📚 Документация проекта

| Файл | Назначение |
|------|-----------|
| README.md | Полная документация проекта |
| QUICKSTART.md | Быстрый старт за 5 минут |
| EXAMPLES.md | Примеры использования API (curl) |
| POSTMAN.md | Инструкции для Postman |
| CONTRIBUTING.md | Руководство для разработчиков |
| PROJECT_STRUCTURE.md | Структура проекта (этот файл) |
| LICENSE | MIT лицензия |

## 🚀 Скрипты запуска

### run.py
Упрощённый запуск сервера:
```bash
python run.py
```

Эквивалентно:
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## 📦 Сборка и деплой

### Локальный запуск
```bash
# Установка
pip install -r requirements.txt

# Запуск
uvicorn app.main:app --reload
```

### Production запуск (будущее)
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

### Docker (будущее)
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0"]
```

## 🔍 Анализ кода

### Качество кода
- Type hints на всех функциях
- Docstrings на всех модулях и функциях
- PEP 8 compliant
- Читаемый и обучающий код

### Покрытие тестами
- Все основные функции покрыты
- Позитивные и негативные тесты
- Параметризованные тесты
- Цель: 80%+ покрытие

### Безопасность
- JWT аутентификация
- Bcrypt хэширование паролей
- SQL инъекции защищены через ORM
- CORS настроен

## 🎓 Учебные цели проекта

Этот проект помогает изучить:

✅ FastAPI фреймворк и асинхронность
✅ SQLAlchemy 2.0 ORM
✅ Pydantic v2 валидацию
✅ JWT аутентификацию
✅ REST API принципы
✅ Pytest тестирование
✅ Структуру Python проектов
✅ Dependency Injection
✅ Git и документацию

---

**Проект создан в учебных целях для изучения современной разработки REST API на Python.**

